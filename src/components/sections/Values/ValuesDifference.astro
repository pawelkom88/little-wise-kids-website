---
import multilingualBubble from "../../../../assets/icons/bubble-multilingual.png";
import heartBubble from "../../../../assets/icons/bubble-heart-with-smile.png";
import artBubble from "../../../../assets/icons/bubble-art.png";
import peopleBubble from "../../../../assets/icons/bubble-people.png";
import outdoorBubble from "../../../../assets/icons/bubble-outdoor.png";
import nutritionBubble from "../../../../assets/icons/bubble-nutrition.png";
import { site } from "../../../data/site";
import "./ValuesDifference.css";

const formatLanguages = (languages: readonly string[]) => {
  if (languages.length === 0) return "";
  if (languages.length === 1) return languages[0];
  return `${languages.slice(0, -1).join(", ")} and ${languages[languages.length - 1]}`;
};

const cards = [
  {
    tone: "blue",
    image: artBubble,
    title: "Screen-Free Learning",
    copy: "Screen-free learning, with meaningful real-world and tactile experiences",
  },
  {
    tone: "purple",
    image: multilingualBubble,
    title: "Multilingual Exposure",
    copy: `${formatLanguages(site.languages)} language exposure for now, subject to change`,
  },
  {
    tone: "green",
    image: outdoorBubble,
    title: "Indoor & Outdoor",
    copy: "A balance of indoor and outdoor learning",
  },
  {
    tone: "yellow",
    image: peopleBubble,
    title: "Child-Led Play",
    copy: "Child-led play that develops confidence and independence",
  },
  {
    tone: "orange",
    image: nutritionBubble,
    title: "Wholesome Nutrition",
    copy: "Wholesome nutrition that supports growing bodies and minds",
  },
  {
    tone: "pink",
    image: heartBubble,
    title: "Family-Style Setting",
    copy: "A warm, family-style environment in Bristol BS5",
  },
] as const;
---

<section
  class="lwk-values-difference"
  aria-labelledby="difference-title"
  data-values-difference
>
  <header class="lwk-values-heading" data-values-difference-heading>
    <span
      class="lwk-values-heading__rays
        lwk-values-heading__rays--left"
      aria-hidden="true"
    >
      <span class="lwk-values-heading__ray"></span>
      <span class="lwk-values-heading__ray"></span>
      <span class="lwk-values-heading__ray"></span>
    </span>

    <h2 id="difference-title">What Makes Us Different</h2>

    <span
      class="lwk-values-heading__rays
        lwk-values-heading__rays--right"
      aria-hidden="true"
    >
      <span class="lwk-values-heading__ray"></span>
      <span class="lwk-values-heading__ray"></span>
      <span class="lwk-values-heading__ray"></span>
    </span>
  </header>

  <div class="lwk-values-heading__dots" aria-hidden="true">
    <span></span>
    <span></span>
    <span></span>
    <span></span>
  </div>

  <div class="lwk-values-grid">
    {
      cards.map((card) => (
        <article class={`lwk-value-card lwk-value-card--${card.tone}`}>
          <div class="lwk-value-card__image" aria-hidden="true">
            <img
              src={card.image.src}
              alt=""
              width="176"
              height="176"
              loading="lazy"
              decoding="async"
            />
          </div>

          <div class="lwk-value-card__content">
            <h3
              class={`lwk-value-card__title lwk-value-card__title--${card.tone}`}
            >
              {card.title}
            </h3>

            <p>{card.copy}</p>
          </div>
        </article>
      ))
    }
  </div>
</section>

<script src="./ValuesDifference.ts"></script>
