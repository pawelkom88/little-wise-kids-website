---
import Container from "./Container.astro";

interface Props {
  id?: string;
  tone?: "plain" | "mist" | "purple" | "blue" | "green";
  width?: "default" | "narrow" | "wide" | "full";
  bleed?: boolean;
  class?: string;
}

const {
  id,
  tone = "plain",
  width = "default",
  bleed = false,
  class: className = "",
} = Astro.props;

const classes = ["section", `section--${tone}`, bleed && "section--bleed", className].filter(Boolean).join(" ");
---

<section id={id} class={classes}>
  <Container size={width}><slot /></Container>
</section>
