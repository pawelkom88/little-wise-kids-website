import type { SeoImage } from '../lib/seo/types';

export const defaultSeoImage: SeoImage | undefined = undefined;

export const defaultSeoConfig = {
  siteName: "Little Wise Kids",
  type: "website",
};
