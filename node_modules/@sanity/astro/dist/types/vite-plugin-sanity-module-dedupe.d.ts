import { PluginOption } from 'vite';
/**
 * Dev-only Vite dedupe for embedded Studio + @astrojs/react islands (#406).
 *
 * Vite can load the same logical package through pre-bundled and direct paths even
 * when npm reports a single copy. Pair resolve.dedupe with optimizeDeps.include.
 * The studio chunk-size warning plugin (apply: 'build') is not involved in dev.
 */
export declare const SANITY_MODULE_DEDUPE: readonly ["react", "react-dom", "react-dom/client", "styled-components", "sanity", "@sanity/ui"];
/**
 * Pre-bundle during dev alongside resolve.dedupe. Includes Sanity-adjacent CJS/deep
 * imports that Vite would otherwise discover lazily.
 */
export declare const SANITY_OPTIMIZE_DEPS_CANDIDATES: readonly ["react", "react-dom", "react-dom/client", "react-compiler-runtime", "react-is", "styled-components", "lodash/startCase.js"];
export declare function resolveSanityOptimizeDeps(projectRoot: string): string[];
export declare function resolveSanityModuleDedupe(projectRoot: string): string[];
export declare function buildSanityModuleAliases(projectRoot: string): {
    find: RegExp;
    replacement: string;
}[];
export declare function vitePluginSanityModuleDedupe(): PluginOption;
