export type DevServerHandle = {
    baseUrl: string;
    port: number;
    stop: () => Promise<void>;
};
export declare function startAstroDevServer({ appDirectory, timeoutMs, disableModuleDedupe, env, }: {
    appDirectory: string;
    timeoutMs?: number;
    disableModuleDedupe?: boolean;
    env?: Record<string, string>;
}): Promise<DevServerHandle>;
