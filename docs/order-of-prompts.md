0. Freeze untouched baseline
   ↓
   0A. Specification MCP initial coverage map — no fixes
   ↓
1. Semantic HTML audit
   → review
   → remediate
   → verify
   ↓
2. WCAG accessibility audit
   → review
   → remediate
   → manual verification
   ↓
3. CSS and JavaScript loading/bloat audit
   → review
   → remediate
   → route coverage and bundle verification
   ↓
4. Performance, Core Web Vitals, assets and loading audit
   → review
   → remediate
   → repeated before/after measurement
   ↓
5. Specification MCP final cross-domain sweep
   ↓
6. Release-candidate verification